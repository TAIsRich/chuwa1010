package com.chuwa.pattern.singleton.demo6;

// Eager Load; Enum Method

public enum Singleton {
    INSTANCE;
}
// If we don't have to worry about the waste of memory space
// This method is our first choice


// And this method is the only one Singleton Implementation that cannot be destroyed
